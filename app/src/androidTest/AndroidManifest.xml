<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="100"
    android:versionName="1.3.0.test"
    >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- merge target -->
    <application android:usesCleartextTraffic="true">
        <uses-library android:name="org.apache.http.legacy" android:required="false" />

        <meta-data
            android:name="com.f_scratch.bdash.mobile.push.channel.id"
            android:value="sdk_test" />

        <!-- must -->
        <meta-data
            android:name="com.f_scratch.bdash.mobile.push.channel.name"
            android:resource="@string/channelName" />
        <meta-data
            android:name="com.f_scratch.bdash.mobile.push.channel.desc"
            android:resource="@string/channelDesc" />

    </application>


</manifest>
